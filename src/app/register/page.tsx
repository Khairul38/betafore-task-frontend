import { Metadata } from "next";

export const metadata: Metadata = {
  title: "BETAFORE || Register",
};

const Register = () => {
  return (
    <div>
      <h1>This is register page</h1>
    </div>
  );
};

export default Register;
