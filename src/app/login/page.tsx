import LoginPage from "@/components/ui/LoginPage";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "BETAFORE || Login",
};

const Login = () => {
  return <LoginPage />;
};

export default Login;
